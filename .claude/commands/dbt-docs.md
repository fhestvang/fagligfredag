Generate and serve dbt documentation.

Execute the following steps:
1. Change to the dbt_project directory
2. Run `dbt docs generate` to build the documentation catalog
3. Run `dbt docs serve` to start the documentation server
4. Inform the user that docs are available at http://localhost:8080
